package org.eclipse.emf.henshin.cpa.atomic;

public class MappingHandler {

	/**
	 * Idee des "zentralen" Mapping Handlers:
	 * Je Analysierter Regelkombination ein Mapping Handler.
	 * D.H eine Instanz die im Konstruktor von "AtomicCoreCPA" initialisiert wird.
	 * Für die vier Mappinggruppen (m1, m2, q12, o1) werden je ein oder zwei HashMaps erzeugt.
	 * Kommen neue Mappings hinzu, so muss dies auch über den Handler geschehen, bzw. dessen Daten müssen angepasst werden.
	 * Alternativ ist es auch denkbar, dass die vier Gruppen von Mappings getrennt verwaltet werden 
	 */
}
